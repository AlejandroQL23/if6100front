# if6100front

abrir la carpeta desde VSC (visual studio code)
Estando en VSC abrir una terminal
correr el comando npm install (esto para agregar los node_modules)
luego ejecutar el comando ng serve
dar ctrl+click en el enlace que sale en la terminal o bien ejecutar el comando ng serve -o (esto abre la ventana en el navegador automaticamente)
